---
services:
    psitransfer:
        image: psitrax/psitransfer:latest@sha256:d145ea03d0e37a80b0a9e10133e00a7304dfa36a0f5e865bc38b4df6c5a7ec63
        user: 1000:1000
        healthcheck:
            test: [CMD, node, -e, 'require("http").get("http://127.0.0.1:3000", (r) => process.exit(r.statusCode < 500 ? 0 : 1)).on("error", () => process.exit(1));']
            start_period: 20s
            interval: 30s
            timeout: 10s
            retries: 5
        ports:
            - 3003:3000
        restart: unless-stopped
        volumes:
            - psitransfer_data:/data
        environment:
            - PSITRANSFER_ADMIN_PASS=${ADMIN_PASSWORD}
            - PSITRANSFER_UPLOAD_PASS=${UPLOAD_PASSWORD}
            - TZ=Asia/Jerusalem
volumes:
    psitransfer_data:
        driver: local
        driver_opts:
            type: nfs
            o: addr=openmediavault.lan,nfsvers=4.2,soft,timeo=600,retrans=2,rsize=1048576,wsize=1048576
            device: :/unreliable/psitransfer_data
