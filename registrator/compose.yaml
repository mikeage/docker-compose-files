---
services:
    registrator:
        image: marioezquerro/registrator:latest
        restart: unless-stopped
        network_mode: host
        volumes:
            - /var/run/docker.sock:/tmp/docker.sock
        command: -ip ${HOSTNAME} consul://consul.lan:8500
