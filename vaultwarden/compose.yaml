---
services:
    vaultwarden:
        image: vaultwarden/server:1.36.0@sha256:d626d04934cd1192ad8ced1adb975099fca78cec33ab467d2d3c923cde7f3b0c
        restart: unless-stopped
        environment:
            DOMAIN: https://vaultwarden.internal.mikeage.net
            SIGNUPS_ALLOWED: 'false'
            ADMIN_TOKEN: ${ADMIN_TOKEN}
            SMTP_HOST: outbound.mikeage.net
            SMTP_FROM: vaultwarden@mikeage.net
            SMTP_PORT: 2525
            SMTP_SECURITY: starttls
            SMTP_USERNAME: ${EMAIL_USER}
            SMTP_PASSWORD: ${EMAIL_PASSWORD}
            TZ: Asia/Jerusalem
        volumes:
            - /opt/vaultwarden/data:/data
        ports:
            - 11001:80
        labels:
            - traefik.enable=true
            - traefik.http.routers.vaultwarden.rule=Host(`vaultwarden.internal.mikeage.net`)
